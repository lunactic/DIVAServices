version: '3.3'
services:
  dev_web:
    env_file:
     .env_dev
    build: .
    command: npm run docker-dev-debug
    ports:
     - "8080:8080"
     - "9929:9929"
    volumes:
     - .:/code
     - /data/:/data
