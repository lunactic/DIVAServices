version: '2'
services:
  web:
    env_file:
     .env
    build: .
    command: npm run docker-debug
    ports:
     - "8080:8080"
     - "9929:9929"
    volumes:
     - .:/code
     - /divadata/data/:/data
